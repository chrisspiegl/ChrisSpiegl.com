This website only contains the mta-sts file for email SMTP server verification/validation/authentication.

To ZIP the folder and upload to cloudflare pages use the following command inside this folder.

zip mta.zip ./* ./.*